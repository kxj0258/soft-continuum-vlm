# data/demos/

Scripted demonstration datasets are written here by `scripts/collect_scripted_demos.py`.

Expected generated files:

- `*.npz`: compressed arrays for observations, actions, rewards, and metadata fields.
- `*.json`: sidecar run metadata with command line, seed, schema, and environment type.
